{
  "name": "seosementara-admin-ui",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('Admin UI static — no compile step')\"",
    "deploy": "wrangler deploy",
    "dev": "npx --yes serve public -l 8788",
    "pages:dev": "wrangler pages dev public --port 8788"
  },
  "dependencies": {
    "tweetsodium": "^0.0.4"
  },
  "devDependencies": {
    "wrangler": "^4.93.1"
  }
}
