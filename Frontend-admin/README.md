# Frontend Admin — HTMX

## Pixel Facebook Pro

Template admin untuk `/admin/pixel/facebook/*`:

- `templates/pixel/facebook/` — halaman Pro (7 tab)
- `static/js/sseo-track.js` — skrip first-party
- `static/pixel.css` — gaya modul pixel

Disajikan oleh Backend Go (`ADMIN_TEMPLATES_DIR`, `STATIC_DIR`).
